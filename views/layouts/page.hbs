{{> header}}
<section class="page"> 
    <div class="container">
        <div class="etapa {{{_sections.slug}}} step-1">
            <div class="simulacao">
                {{{_sections.simulacao}}}
            </div>

            <div class="texto">
                <p>{{{_sections.texto}}}</p>
            </div>

            {{> botoes}}
        </div>
    </div>
</section>
{{> footer}}